# Copy this file to .env and fill in your own keys.
# NEVER commit the real .env file — it is ignored via .gitignore.

OPENAI_API_KEY="your-openai-api-key-here"
NGROK_API_KEY="your-ngrok-auth-token-here"
